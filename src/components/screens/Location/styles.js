import { StyleSheet } from "react-native";

export default StyleSheet.create({
  centerText: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  },
  fontSizeText: {
    fontSize: 23,
    fontWeight: "bold",
    color: "#000"
  }
})